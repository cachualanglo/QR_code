# Backend Rules

Implement approved OpenAPI exactly. Entity is not DTO. Controller contains no business logic. Service owns business rules and transactions. Backend enforces permissions. Never expose secrets. Tests must cover business rules, security, persistence, and API contracts.
