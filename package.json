{
  "name": "ai-project-factory-e2e",
  "private": true,
  "scripts": {
    "e2e": "playwright test",
    "e2e:smoke": "playwright test --grep @smoke",
    "e2e:critical": "playwright test --grep @critical",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "latest"
  }
}
